import React, { Component } from 'react';

export function ButtonBreak(props) {
  const style = {
    margin: "0 0.5rem",
    width: "1px",
    background: "rgb(var(--pale-grey))"
  };

  return <div style={style}/>;
}
